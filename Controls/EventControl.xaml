<UserControl x:Class="SportsTeamManagementApp.Controls.EventControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SportsTeamManagementApp.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Border Style="{DynamicResource EventControlElement}">
        <StackPanel Margin="10">
            <TextBlock Text="{Binding Name}" FontSize="25" FontWeight="SemiBold" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding Date}" Margin="10,5,0,0" FontSize="20" FontWeight="SemiBold" HorizontalAlignment="Left"/>
            <TextBlock Text="{Binding Time}" Margin="10,5,0,0" FontSize="20" FontWeight="SemiBold" HorizontalAlignment="Left"/>
        </StackPanel>
    </Border>

</UserControl>
